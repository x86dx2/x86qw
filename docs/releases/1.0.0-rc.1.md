# x86QW 1.0.0-rc.1

Release candidate público para validação operacional do x86QW.

## Identidade

- commit de produto: `a8758ee27bebd7c72c24a31dc19335652e260c0a`;
- linha auditada: `main@335d9a062f8ce33b226a9892de82979828a0fd1b`;
- run final de promoção: `31752738047`;
- release: [GitHub](https://github.com/x86dx2/x86qw/releases/tag/x86qw-installer-1.0.0-rc.1);
- instalador: `x86qw-installer-1.0.0-rc.1.zip`;
- instalador: 600431 bytes;
- SHA-256 do instalador:
  `9600be7eb2ed14e23b2eeb079bd6aa0e4611f996be0c89741fda12587eb7fed8`;
- `candidate.json`: 14474 bytes;
- SHA-256 de `candidate.json`:
  `1552a896a0076dd2e347ed5b732b6dd31ba892292e1f9fb8c97fe9111f755bcb`.

## Estado de plataforma

- macOS arm64/M3 stable: `conditional`, com evidência funcional nativa;
- MVDSV, QTV e QWFWD macOS arm64: candidatos a suporte durante o soak;
- macOS nightly: `preview`;
- macOS Intel: `preview`;
- Linux: `preview`;
- Windows: `preview`.

`conditional` não significa Gatekeeper, notarização ou identidade upstream
original comprovados. `preview` não significa execução nativa aprovada.

## O que testar

1. executar o workflow [Public release acceptance on Apple M3](../../.github/workflows/public-acceptance.yml)
   contra os endpoints públicos;
2. instalar pelo bootstrap e catálogo públicos em diretório descartável;
3. executar `version`, `verify`, `changes`, `migrate --dry-run` e `update`;
4. jogar KTX, Final Arena, Pro-X, Team Fortress e TD2;
5. testar MVDSV, QTV e QWFWD;
6. reportar qualquer falha P0/P1 com plataforma, commit, hashes, logs e passos
   de reprodução.

O período de uso deve incluir uma trajetória real `0.7.13 → 1.0.0-rc.1`,
atualização idempotente, reparo de fixture corrompida, desinstalação e purge.

## Limitações conhecidas

- a release GitHub ainda não está marcada como host-level immutable;
- a evidência assinada usada na promoção deste RC ainda depende do artifact
  protegido; candidatos futuros materializam
  `release-evidence.json`, `evidence-root.json` e `release-receipt.json` como
  assets duráveis;
- o RC não é `Latest` e não substitui os bytes estáveis de `0.7.13`;
- a promoção de `1.0.0` permanece bloqueada até concluir o soak e repetir os
  gates sobre um candidato final novo.

## Mudanças incompatíveis

O RC usa a linha de migração `0.7.0–0.7.13`. Antes de atualizar uma instalação
real, preserve o diretório de dados e valide o plano com `migrate --dry-run`.

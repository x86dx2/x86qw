{
  "name": "x86qw-site",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.5.2",
  "scripts": {
    "deploy:dry-run": "wrangler deploy --assets ./public --dry-run",
    "dev": "wrangler dev --assets ./public --ip 127.0.0.1 --port 8787",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "playwright": "1.62.1",
    "wrangler": "4.126.0"
  }
}
